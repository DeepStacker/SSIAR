services:
  app:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      AZURE_DOC_INTELLIGENCE_ENDPOINT: ${AZURE_DOC_INTELLIGENCE_ENDPOINT:?err}
      AZURE_DOC_INTELLIGENCE_KEY: ${AZURE_DOC_INTELLIGENCE_KEY:?err}
      MAX_WORKERS: ${MAX_WORKERS:-4}
      SURYA_ENABLED: "0"
    volumes:
      - /data/shared:/app/shared
    restart: unless-stopped
